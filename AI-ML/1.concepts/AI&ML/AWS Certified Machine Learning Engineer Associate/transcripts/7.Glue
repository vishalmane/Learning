All right.

Let's talk about AWS glue, which is a system for building your table definitions and performing ETL,

extract, transform and loading of your data automatically.

Now glue is a huge deal on the exam right now.

So back when they removed all the machine learning content from the AWS Big Data Exam, it was called

at the time into its own exam for machine learning.

They kind of filled that gap mostly with more content about glue and redshift.

So glue really plays a major role in the exam today.

So it's very important to pay attention to this section.

There's not really a whole lot you need to know about glue, but you'd need to really internalize it

and understand how it connects all these different services together and how it might be used as pieces

of larger applications.

So what is glue all about?

Well, it's a serverless system.

So again, you know, there's nothing you need to maintain there that will automatically handle discovery

and definition of table definitions and schema.

So its main use is to serve as a central metadata repository for your data lake.

You will discover those schemas out of your unstructured data, sitting in S3 or whatever, and publish

table definitions for use with analysis tools such as Athena or Redshift or EMR.

So again, the purpose of glue itself is to extract structure from your unstructured data.

If you have data sitting in a data lake in S3, it can provide a schema for that so that you can query

it using SQL or SQL like tools, including RDS and Redshift and Athena and Amazon EMR, and pretty much

any other SQL database that can use a schema like that.

The other thing glue does is custom ETL jobs.

So once it discovers that schema, it can do some processing of it for you.

And this can be trigger driven based on when that data is received or on a schedule or on demand.

And it's fully managed.

So again, there's no services for you to maintain, no hardware for you to maintain the ETL jobs use

Apache Spark under the hood.

We haven't talked about that yet, but spark is a very popular system for distributed data processing.

But you don't need to manage the spark cluster, which is really nice.

So you have the entire power of Apache Spark behind you with glue ETL without having to manage that

spark cluster, which is a really, really nice thing.

So one piece of it is the glue crawler and data catalog, which it populates.

So the purpose of the Amazon glue crawler is to scan data in S3.

And often it will infer that schema automatically just based on the data that it sees sitting there.

But sometimes you have to give it some hints.

You can schedule that to run periodically if you need to.

And what that does is populate the glue data catalog.

So we have data sitting in S3.

Glue can periodically or on demand scan that data and construct a schema of it.

Again that's just the table definition.

You know, what are the columns of data in that information that you have stored there in an unstructured

manner?

What's their data types?

Things like that.

Column names, it can infer that as well, where it's stored.

That's all important information as well.

And it uses that information to allow things like redshift, Athena or systems running on EMR like hive

to query your unstructured data in S3 as if it were structured data just like it were a traditional

relational database.

Now the data itself remains where it was originally, so the data stays in S3.

You're not duplicating that data.

We're not copying it into an actual database table somewhere else.

We're just using glue to basically provide the glue between these relational database interfaces and

the unstructured data lake that's sitting back here in S3.

Once cataloged, that data is immediately available for analysis, and we'll build this out and go into

a lot more detail into these systems later on.

And then, you know, once you actually have that data sitting in something like Athena, you can use

something like Amazon QuickSight to visualize that data.

So glue again is the glue between your unstructured data and structured data analytics tools.

So let's talk about glue and S3.

Partitions really quickly here.

So it's not entirely magic.

You know you do need to think about how you're going to structure your unstructured data.

I know that sounds backwards, but the way that you organize your data will affect how efficient glue

can work and how efficiently your systems can query this unstructured data.

Now, the glue crawler will extract partitions of your data based on how your S3 data is organized.

So you want to think up front about how you're going to be querying your data lake in S3, and what

those partitions should represent for the best performance.

So, for example, let's imagine that you have devices sending sensor data into your system, into your

data lake every hour, just dumping raw data into S3.

So think about how are you going to use that data.

Are you querying primarily by time ranges?

If so, then you want to partition based on those time ranges, right?

So you'd want to organize your S3 buckets as year, followed by month, followed by date, followed

by device.

Okay.

That way, you know, we can start by winnowing things down by the year and further by the month, furthermore,

by the date, and then finally by the device that generated that data that will allow the partitions

to really allow us to efficiently access data for a given date without having to slog through all the

other information for other dates that we don't care about.

On the other hand, if you're querying primarily by device and not by time, you might want to put the

device up first.

That way, your primary partition would be the device ID and then followed by the year, month, and

date.

So if you're going to be querying for specific individual devices all the time, and that's kind of

the primary thing you want to partition on, you'd want to make that the top level bucket that you're

organizing your data under within S3.

So this is something that you might see in the exam.

What's the optimal way to organize your data that's being stored on S3 such that glue can extract that

and present it as partitions that make sense for what you're going to do with it Ethan.


There are a few new features in AWS glue.

They seem to be investing a lot in this feature right now, so not sure you're going to see this on

the exam quite yet, but you probably will soon.

So one of these new features is called AWS Glue Studio.

And basically it's a visual interface for defining and creating ETL workflows.

So it allows you to create these more complex workflows where you might have multiple things happening

in parallel from the same source, or maybe different sources being processed in parallel in different

ways.

But it lets you set up these complex jobs in a visual manner, and often without writing any code.

So it offers you this visual job editor that I'll show you in a moment, that allows you to create directed

acyclic graphs for complex workflows like this.

And the sources of that currently can can include S3 or Kinesis or Kafka or any JDBC source as well.

You can then graphically set up transforms of that data in the Glue Studio UI.

So you can do transformations, you can sample the data, you can join tables together and a few other

things as well.

And then you can take the output of that transform and send it to S3 or the Glue data catalog.

It also supports partitioning out of the box.

So if you do have partitioned data, it will automatically take advantage of that and accelerate itself

accordingly.

Also, it offers a visual job dashboard that lets you visualize the status of your glue ETL jobs and

how they're running, what's running, how long they've been taking.

So that can be useful for quick troubleshooting and making sure that your jobs are running as expected.

Let's dive in and show you how it actually works.

So let me give you a little bit of a quick tour here of Glue Studio.

It's a little bit hard to find these days because in some places they call it Glue Studio and other

places they call it visual ETL.

You can get there a couple of ways.

One would be from this go to ETL jobs button here, or from the little sidebar here.

It's under visual ETL under ETL jobs.

And you can see now we're calling Glue Studio again.

Let's click on the visual ETL button just to give you a little bit of a tour here.

And you can see that the list of sources has grown considerably over time.

So it's not just glue, Data catalog, S3, Kinesis, and Kafka, pretty much any data source you can

imagine, even if it's outside of Amazon, is fair game.

So.

So any external database, MySQL, Microsoft SQL, whatever you can imagine, even snowflake, Google

BigQuery, you know, things from competing vendors, even Azure, uh, MongoDB, zoom, third party

tools like QuickBooks or LinkedIn.

I mean, I think even Salesforce PayPal is in here somewhere.

The list goes on.

So pretty much any data you can imagine is fair game for Glue Studio.

So we start off by adding a node for a source.

Let's just keep it simple and say it's something that I have in S3.

Just click on that node for the data source and tell it where it is.

So I'll just pick one of the data sources that we had from earlier in the course under order logs.

Order logs Sundog soft.

So that's where that data is coming in.

And now if we look at the output schema it will tell me what that data looks like okay.

So now we can do stuff to transform that data.

Let's go ahead and click on that and click on plus and add a transform node.

And you can see there's many many different things you can do here as well.

So we can do changing the schema SQL queries, uh, filters, droppings, things.

There should be even some capabilities for writing your own custom code somewhere.

Yeah, there it is.

Custom transform.

But let's do something simple and just change the schema, which lets you do a lot of common things.

So let's say I just want to drop the description field, uh, change the year to an integer.

Change the date to an integer.

You know, whatever you want to do.

I'm just just playing around here and that would do what you expect.

So click on that.

And you can also look at the output schema there as well to make sure it did what you said.

The description is gone year and day are now an integer.

And let's also apply a filter to that.

So let's do a transform filter.

And we could say for example add a condition.

Let's say we want to filter everything out where the year is less than 2001.

It's just that simple.

All right.

And click on that.

And you can see the output schema is the same.

We just filtered out some data and now we can put that somewhere.

So let's click on plus again and add a target.

And let's just dump that in S3 as well.

Again, you can see that there's many other places you can send this to, including snowflake, BigQuery,

Azure.

They don't care.

So pretty amazing.

But let's just pick another S3 bucket.

And then I could specify what bucket I want to put that in right.

And by the way it doesn't have to be linear like this.

So this is actually a directed acyclic graph.

So you could have things happening in parallel alongside each other.

I could click back up here and do that filter in parallel instead of after that transform node if I

wanted to.

So you can have these more complicated structures where processing is happening in parallel to make

it even more efficient.

If that makes sense for what you're doing.

It doesn't have to be linear like this.

All right.

So that's Glue Studio just to kind of show you what it is and what it can do.

Let's go back to the previous page.

And from here you can monitor your existing jobs and see what they're doing.

Last time you changed them and everything else.

And they also have some examples you can look at as well.

So that's Glue Studio.

You know I think they're kind of pushing people more toward data Wrangler these days.

But it's still there for you.

Also known as visual ETL.

In.

More recent feature in Glue Studio is AWS Glue Data Quality.

This is basically a step you can inject in your glue job to automatically evaluate the quality of the

data coming in, and if it violates certain parameters or rules that you set up, then it can automatically

fail the job or just log something in CloudWatch for you.

Now you can create these rules either manually, or they can create them for you automatically.

So you can actually tell glue data quality.

Hey, go look at my source data.

I know this is good data here.

Infer some rules from it.

You know what's the expected range of the data that I'm seeing here?

What's the standard deviation of typical values that I'm seeing?

Should I have alarms in place if I if I exceed those ranges.

Right.

And once you set up those data quality rules, you can just integrate them right into your glue jobs,

like you can see in this diagram to the right.

So just like another transformation stage there, one of your stages could be evaluating the data quality

using glue data quality.

Now if you are going to be creating these rules by hand, it uses something called the Data Quality

Definition language or dcdl.

And we'll look at some examples of that on the next slide.

And again you can have this do a couple of different things.

You can set it up so that if the data quality rule fails, it will fail the whole job.

That can be annoying though, because if you do have sort of a a threshold that's a little bit too tight,

you can get a lot of false positives there.

Alternately, you can just report the results to CloudWatch where you can interpret them and act on

them as you see fit.

A little bit of an example here of what it looks like in the glue UI.

So here they've created a rule called rule set one Rs1.

And you can see down there some examples of the Dcdl language for those rules.

And this was created by the automatic recommendations.

So you can see that you can run glue data quality on your data set.

And it will automatically create these dcdl rules for you.

And you can go in there and edit them if you want to.

If you don't agree with the things that I came up with.

So there's some examples here.

It's saying that the row count should be between these values.

If I'm seeing less than the expected number of rows, something might be wrong.

You can test to make sure that you have complete data in certain columns.

You can test to make sure that the length of certain columns is within a certain range of values.

There.

Things like that.

Right.

Standard deviation also.

That's an interesting one.

So you can say, well, if the date of infraction or the ticket number is outside the expected bounds,

we should also do something about it okay.

And you can see after you actually run that on the right here what the results would look like.

So in this example here, the Rs1 rule set that we made failed because the standard deviation on date

of infraction and the column value seen in date of infraction were not as expected.

Again, these are examples of where the thresholds can be a little bit too tight and lead to potentially

a false positive.

But maybe it's a real issue.

Maybe there's some bad data in there or some outlier that needs to be accounted for.

That would mess up your analysis later on.

So that's AWS glue data quality, relatively recent feature.

It should be fair game in the exam from say, 2024 onward though.


Let's talk about glue Databrew.

This is definitely something you need to know in the field of data engineering, right?

Because this is a visual data preparation tool.

So it's another UI that you can use just for transforming data, doing the tea of your ETL.

So very, very very relevant to this exam.

It is a UI for pre-processing large data sets.

Those data sets can come from S3 from a data warehouse or any database you want.

The output, however, will go to S3 once it's been transformed by Databrew, and there are over 250

ready made transformations that you can visually apply to that data.

So you can just kind of drag and drop these things in Databrew and have it automatically transform your

data in pretty much any way you can imagine.

And I'm not going to go through all 250 of them.

If you do want to go through the documentation on AWS's website and at least scan through them, it

could hurt.

You know, it's good to know what the capabilities are, but the short version is pretty much anything

you can dream up is probably in there.

You can create these recipes of transformations that can then be saved as jobs within a larger project.

So if you have multiple transformations you want to apply to your data, you can create a recipe of

those transformations and then save that off and apply that to different places, different projects,

different data sets.

Those recipes are made up of recipe actions.

And if you go to that documentation for the transformations, you'll see those definitions of those

recipe actions.

So as an example here on the right this is a recipe action called nest to map.

And this is used to take selected columns and convert them to key value pairs.

So instead of individual columns that might convert it into a JSON document that has these key value

mappings between those columns and their values.

So I don't know why you'd want to do that, but you can do that.

And you can also optionally remove the source columns that you combined into that map as well as part

of it.

So that's what's going on here in the parameters.

We're specifying what source columns we want to nest.

So we're going to take the age the weight kilogram and height centimeter columns.

We're going to put them into a new single column called column name, where it's just going to be stored

as this JSON based map that will say, for example, age equals 53 weight, kilograms equals 100 height

centimeters equals, you know, 500, whatever it is.

Right?

You can tell I don't really deal with the metric system because I'm sure those numbers make no sense.

And then we also say remove source columns.

So the original age weight and height columns will be deleted as part of that nest to map transform.

You can also define data quality rules as part of Databrew as well to kind of make sure that your data

makes sense as you go along.

And you can also create data sets with custom SQL from Redshift and Snowflake as part of Databrew as

well.

From a security standpoint, Databrew does integrate with KMS using customer master keys only.

It of course uses SSL in transit, and you can use IAM to restrict who can do what.

As with most services and also as with most services, it integrates with CloudWatch and CloudTrail

for alarming and governance and all that good stuff.

Something the exam guide calls out is handling personally identifiable information in your Databrew

transformation.

So let's have a little bit of a diversion into that world.

We talked about some of these techniques earlier in the course, but just to reinforce it and how it

relates to Databrew one way of dealing with PII is with substitution.

So I could just replace that with some sort of random numbers.

Right.

And substitute that information with something that is not personally identifiable.

I could shuffle it.

I could take all of the PII values in my rows and shuffle them all together so that Person A's credit

card number might be associated with person B instead with credit card numbers, though probably not

a good idea to have raw credit card numbers floating around at all.

Deterministic encryption is one way of encrypting that personally identifiable information in such a

way that a given value always results in the same encrypted value.

It's deterministic.

That way you can also have probabilistic encryption, and that's a system where there might be more

than one result from an encryption of a given field.

You can also decrypt something that was encrypted using a decrypt command, and there's probably no

better way of dealing with PII than not having it in the first place.

If you want to null it out or just delete it.

The delete transformation can be used to just drop that information entirely.

You can also mask out personally identifiable information.

So a lot of times you'll see only the last four digits of a credit card or a social security number

being displayed where the rest of it is masked out or scrambled in some way.

Masking is a way to do that.

And finally, there's hashing, where you apply some sort of a hash function to the value in a cryptographic

fashion.

Hashing is a little bit different, though, in that multiple values can hash to the same result.

So sometimes that's okay, you know, but it does provide some anonymity to what you're doing as well.

So some techniques for handling personally identifiable information in databrew.

Transformations.

And these are the specific names of those transformations that are associated with each technique.
Let's dive into Amazon Athena next.

That is a serverless interactive query engine for S3 data.

So pretty cool stuff.

You can take potentially a massive data lake in S3 and just put Athena on top of it and query it just

like it were a SQL database.

So let's dive into what it's all about.

What is Athena?

Technically it's description as an interactive query service for S3.

So again, it's basically a SQL interface for your data in S3.

There's no need to load that data into some intermediate database.

The data just stays put in S3.

And Athena knows how to interpret that and query it as though it were an actual database.

Under the hood it uses Presto, but you don't really need to know that.

It's just kind of what they started building Athena on top of.

It's evolved quite a bit since then, though, so its roots may have been in Presto, but it's really

more of its own thing these days.

The big thing about Athena, though, is that it is fully serverless, so you don't need to think about

the underlying servers or capacity or anything like that.

You just go to Athena, point it at your data in S3, and start writing queries.

Well, there's more you can do than that, but you could just do that.

That's all there is to it.

You just go to the UI, the the console for Athena and go to town.

You don't have to think about setting up all the underlying capacity.

It's all abstracted away for you.

It supports many different data formats, so your data in S3 can be pretty much anything you can dream

up, whether it's comma separated value or tab separated value.

Those are both legitimate.

So any human readable data, you can put Athena on top of it and it can make sense of it and query it.

JSON format also supported ORC columnar format also supported.

Good thing about ORC and parquet is that they are both splittable formats, and that can be useful for

improving performance and segmenting your data in ways that make sense for your queries.

Avro also Splittable and as far as compression goes, you can store your data in snappy zlib ae or gzip

compression.

Now there are some takeaways here you should keep in mind for the exam.

Remember Orkin Parquet are columnar and splittable formats.

Avro is only splittable.

CSV, TSV and JSON are all human readable formats.

Okay, so csv, tsv, JSON you can open up that file in a text editor and make sense of it.

ORC parquet Avro not so much.

Avro will be splittable, so you can process Avro data in parallel, because Athena can split that up

and chew away on it in parallel in different chunks, right?

Orc and parquet, in addition, are columnar formats, and that can accelerate queries that are only

specific to certain columns.

And pretty much any compression you can dream up is supported as well.

So for the exam purposes, those are the things you want to remember.

The data itself can be unstructured, semi-structured, or structured.

Athena doesn't care.

So whatever level of schema you might have, it can take advantage of it, or it can infer a new schema

just by examining the data or taking one that you've provided to it.

Some examples of usage here are given by Amazon.

Include the ad hoc querying of web logs.

So Athena is given as a better example of how to deal with querying web log data in S3, and they would

rather have you use this than Elasticsearch, for example.

You can also use it for querying staging data before loading it into redshift.

So maybe you have a bunch of data being dumped into S3 and you want to transform it and load it into

a big redshift data warehouse.

But maybe you want to be able to play around with that data and investigate it and see what it looks

like beforehand.

Athena might be a good way of sort of getting a bigger picture of what that data looks like before you

actually commit it into a data warehouse.

It's also useful for looking at other logs besides web logs in S3, including CloudTrail logs, CloudFront

logs, VPC logs, elastic Load balancer logs, whatever you have.

If it's in S3, Athena can query it.

It also offers integration with tools like Jupyter and Zeppelin and RStudio notebooks, because you

can just treat it like a database.

It has ODBC and JDBC interfaces, so you can treat Athena like any other relational database that you

might interact with or integrate with.

That also includes QuickSight.

You can also integrate Amazon's QuickSight visualization tool into Athena.

To actually use that as sort of the glue.

Well, glue is a poor choice of word because we are actually using the glue service for this.

But the thing that connects your unstructured data in S3 to a more structured visualization or analysis

tool, such as QuickSight.

So speaking of glue, let's talk about how Athena integrates with AWS glue to actually impart structure

on your unstructured data in S3 so that it can actually query it like a database.

So again, let's do a little brief recap on how glue works.

You might have a glue crawler populating the glue data catalog for your S3 data.

That's looking at what's stored in S3, and trying to extract columns and table definitions out of it

for you.

And you can use the glue console to refine that definition as needed.

So once you have a glue data catalog published for your S3 data, Athena will see it automatically and

it can build a table from it automatically as well.

So anytime Athena sees something in your glue data catalog in your account, it's going to make a table

for that for you so you can query it just like you would any other SQL database.

And it's not just Athena that can use that glue data catalog either.

It will allow any other analytics tool to visualize or analyze that data as well.

For example, RDS, redshift, redshift spectrum, EMR, any application that's compatible with an Apache

Hive metastore as well.

Because remember the glue data catalog can be used as a hive metastore too.

But in this example we're using it to expose that table definition to Amazon Athena.

And with Athena integrated with AWS Glue Data Catalog that allows you to create a unified metadata repository

across various services.

Crawl data to discover schemas, populate your catalog with new and modified table and partition definitions,

and maintain schema versioning all under the hood.

And Athena just sits on top of that and provides a SQL interface to that underlying glue structure.

Another feature of Athena is work groups, and you definitely need to know what these are all about.

Basically, it's a way.

It's what it sounds like.

You can organize users and teams or even applications and workloads into different work groups and group

them together.

And you can use this to control the query access of a given work group or to track costs by a given

work group.

So maybe you have a specific team of people that should only have access to specific types of queries,

or limits on the queries that they can run.

Right.

And you want to track their costs individually to figure out what kind of bill they're running up for

the organization.

Work groups are integrated with IAM, CloudWatch, and SNS, so you can do things like have it notify

you when limits are hit.

And the way you use this is you can have your own data limits on each work group, so you can limit

how much data queries can scan by work group.

So if you want to make sure that somebody in a given team isn't going to construct some wildly inefficient

query that has this huge bill that results, you can limit that at the work group level.

Each work group also has its own individual query history.

So, you know, if you want to make sure that a specific team of people isn't seeing the queries that

another team makes, you can break them up into work groups to make sure that each work group only sees

its own query history.

Work groups, of course, can also have their own IAM policies, because that's how you're going to

manage their permissions and what they can see.

And you can also have your own encryption settings on each work group as well.

So it's a good way of organizing many people in an organization and limiting what they can do and giving

them specific access permissions by using work groups and Athena.

So just remember, Athena work groups are a thing.

You can use them to limit the access of a given group of people, or to measure the costs that they're

doing, and to have limits on the amount of data that their queries can actually scan.

The cost model is very simple because it is a serverless application.

You just pay as you go for the activity that you actually consume.

It charges you $5 currently per terabyte scan, which is pretty generous.

Important to remember that successful or canceled queries do count toward that scanned number there.

You do get charged for those, but any failed queries are not charged, so any successful or canceled

queries you will be billed for.

Failed queries, however, are free.

There's also no charge for any DDL operations such as create, alter or drop.

Those are free as well.

Now a very important point is that if you want to save money using Athena, it can save you a lot of

money by converting your data to a columnar format like we talked about with ORC and parquet.

So not only does that give you better performance for applications that are typically querying on a

small number of columns, it can also save you 30 to 90% in terms of money as well.

That's because it allows Athena to selectively read only the required columns when doing a query and

processing your data.

So if you're only accessing certain columns from your queries by having a columnar format, you're reducing

the amount of data that you actually have to scan with Athena.

And remember, you are charged by terabyte scan.

So by reducing that scanning you win.

So remember Athena works best with column Reformats it can save you a lot of money and give you better

performance as well.

Examples of columnar formats include orc and parquet.

All right.

And in addition to Athena, of course, glue and S3 have their own charges as well.

Athena just sits on top of glue to get the table definition for your S3 data, and your data is still

being stored in S3.

So there are separate charges for glue and S3 in addition to using Athena as well.

Also, I should point out that partitioning your data can also help Athena to reduce costs as well.

So if you do have your data structured in S3 in different partitions such as by date or hour or something,

queries that are restricted to that given partition will also scan less data as well.

So in addition to using a columnar format, partitioning your data within S3 can also save you money

with Athena as well.

Let's talk about security two.

That's always important with Athena.

It offers many different ways of securing your Athena traffic.

One is through access control, who can actually get to Athena and query your your information in the

first place.

And you can use IAM access control lists and S3 bucket policies to restrict access to that information.

The IAM policies that are relevant here are the Amazon Athena Full Access and AWS QuickSight Athena

Access policies.

You can also encrypt your results if you are sensitive about the results of your queries, and you can

encrypt them at rest in a staging directory in S3, and that can be encrypted in several different ways.

You can.

Again, these are important to remember guys, so you can encrypt your S3 data results in server side

encryption using an S3 managed key that is called ssh S3.

You can also do server side encryption of your results using a KMS key that's called ssh kms.

Or you can encrypt them on the client side with a KMS key as well.

That's called csc kms.

And depending on your security requirements, one or more of these may make sense.

So think about how that data is flowing, where it's being stored and how you want it to be secured.

You can also have cross-account access defined in S3 bucket policy.

So it is possible for you to access Athena to a S3 bucket that is not owned by your account.

If that other S3 bucket has a policy that grants access to your account, it's possible for an Athena

console in one account to access a data lake stored in another account.

You can set that up as far as In-transit security goes.

You can use TLS, the transport layer security for all the traffic going between Athena and S3 as well.

That can be set up to for In-transit security with Athena.

Always important to remember security aspects guys.

It's a huge part of the exam.

Finally, let's talk about Anti-patterns.

Again, these come straight out of the AWS Big Data whitepaper of things they don't want you to use.

Athena for one, is for highly formatted reports or for visualization at the end of the day.

Athena is just a SQL query engine.

If you want to do nicely formatted stuff for visualizing things with charts and graphs, well, that's

what QuickSight is for and we'll be getting to that shortly.

Also, if you want to be doing ETL, extract, transform and load operations using Athena, that's generally

not the best tool to use.

Glue is an alternative there and using glue ETL instead.

You can also do that with Apache Spark or what have you for larger scale tasks.

So that's Athena in a nutshell.

Let's go and play with it, shall we?

Let's talk about the Create table as select statement or CTAs for short.

You will probably see this in the context of Amazon Athena, although this is also a thing in other

databases as well.

The syntax might be a little bit different, though very similar in spirit to a materialized view in

redshift.

It creates a new table from query results.

So you can do any Select statement you can imagine, and create a new table from the result of that

query.

And maybe you can use that to create a new table that's a subset of another table.

So I can say hey I'm going to go do a select from this old table for some subset of what's in that table,

and create a new table based on that query result.

However, it's also a neat little trick for converting data into a new underlying format, and this

is especially interesting remembering that Athena can sit on top of a data lake in S3, so you could

use this as a way to convert data in S3 to a totally different format that might be more performant

for Athena, right?

So, for example, let's take a look at these two examples down below.

Here, the one on the left we're saying create table new table with format equals parquet right.

Compression equals snappy as select star from old table.

So what's going on here.

We're going to select everything from old table.

And we're just going to rewrite it into a table called new Table.

But along the way we're going to change it to parquet which is a columnar format that's going to be

better optimized for doing queries in Athena, and also with snappy compression to further optimize

it as well.

So this is a little easy way to very quickly compress or change the format of your raw data in S3 and

changing it to something that works much more efficiently with Athena.

Another example on the right here, where same spirit.

Here we're selecting everything from old table and putting it into a new table called my ORC task table.

In this case, not only are we changing the format to ORC along the way, we're also specifying a new

external location.

So I'm saying, okay, not only do I want to change the format for this table, I'm going to explicitly

store it within this S3 path.

Within that my Athena results bucket.

Right?

So that's kind of an interesting trick because not only are we changing how it's exposed to Athena itself,

we are also changing and storing the underlying raw data into this other table in S3.

So that might be a quick and dirty way of doing data transformations or transforming the actual format

of the underlying data, the raw data, into something else, just using Athena.

And then I can go to that S3 bucket and pick it up there if I want to.

So create table as select.

Very handy trick for converting data into something more performant, and maybe even putting it off

somewhere where you can get it later.

Don't forget that.
A few quick notes on optimizing Athena's performance.

The exam allows to focus on topics like this.

Uh, basically, it comes down to three things that we want to talk about here.

One is you're usually going to get better performance by using columnar data formats like Orc or parquet.

Uh, Athena will just work better if you can pre-process your data somehow to be in a columnar format.

And you can do that with things like glue or through an ETL transform.

Uh, you know, there's a lot of tools you can use to transform your data into a format that's more

optimized for Athena's use.

So if you are concerned about performance, uh, make sure that you're using a columnar data format

under the hood with Athena and Orc or parquet.

So if it's hitting S3 data, if it's in that format already, it will save you quite a bit of work.

Also remember, a small number of large files will generally perform better than a large number of small

files with Athena.

So if you can have a small number of large ish columnar data files, that's probably going to be your

optimal performance case right there.

Also, take advantage of partitions.

So if you're able to organize your data in S3 in a partition format, that lines up with how you're

going to be querying that data.

So for example, if you're, uh, hitting up your data most commonly by a specific date range or something

like that, then partitioning your data by date would make a lot of sense right now.

If you do need to add partitions after the fact, let's say that you have an existing data set in S3

that is not partitioned, and you want to improve its performance by adding partitions.

You don't have to totally start from scratch.

What you can do in Athena is use a command called MSK Repair Table to go back and add that metadata

for the partitions into Athena.

So if you do need to add partitions later on to an existing Athena database, you can use MSK repair

table to accomplish that.

So those are the main points about Athena performance.

Just remember those three things and you should be in good shape.

A new feature in Athena is Acid transaction support.

That means that we have strict guarantees about transactions, so you can actually have concurrent users

hitting the same row at the same time and have it all work consistently.

Under the hood is powered by Apache Iceberg, and to use it, all you need to do is add table type equals

iceberg.

When you're creating a new table in Athena using the create table command.

And this allows you, like I said, to have concurrent users safely making row level modifications or

deletions, whatever it is, without worrying about them stomping on each other.

This is actually compatible with elastic, MapReduce, Apache Spark, and anything that supports the

Apache Iceberg table format.

The good thing is that it removes any need for custom record locking, so without acid support, you

would need to have some sort of a solution for locking a record and unlocking it, surrounding any modifications

to it, so that if two people were hitting the same record at the same time and trying to write to it,

they wouldn't be fighting with each other.

Iceberg and acid transactions just does that for you automatically now.

Also, a little side benefit is that it gives you what's called time travel operations.

So you actually have the ability to recover data that was recently deleted just using a plain old Select

statement.

So some of the historical data that's keeping around for the purpose of asset transactions can be used

for that purpose.

Now remember when we talked about governed tables in lake formation.

Well this is kind of another way of doing the same thing.

So governed tables gave us acid support on S3 tables in lake formation.

But this is another way of getting acid features with Athena.

So you could set up a governed table in lake formation and then hit that table with Athena and you would

have acid support.

This is just another way of doing it.

So you can just create a table explicitly from Athena directly with a table type of iceberg, and you'll

get an acid support on that as well.

So kind of two different ways of solving the same problem there.

Now you might also remember that in lake formation with governed tables It had a feature to automatically

compact those tables over time, because over time there's sort of this overhead that builds up and

it can actually make your performance degrade as time goes on.

Now, with Athena acid, at least for now, you do need to do that yourself periodically.

So if you are using Acid transactions in Athena, you'll probably want to do a command like this once

in a while.

Optimize table rewrite data using bin pack where catalog equals whatever.

So a command like that will go back and compact your data.

That's sort of been bloated by this acid support over time and make it performant again.

So that's the kind of thing I could see showing up on the exam that that kind of trivia.

What do you do if you're acid?

Transactions are getting slower over time?

Well, the answer would be to periodically compact them using the bin pack command here that you're

seeing.

That's Athena acid in a nutshell, an exciting new feature in 2022, as more and more of the AWS data


analytics world goes to Acid support.

Let's talk about Athena.

Fine grained access to AWS Glue data catalog.

It's a not fine grained in the traditional sense.

The idea is that we have IAM based database and table level security for specific operations.

So this is broader than data filters.

In lake formation.

We're not applying cell level security or row level security or column level security here.

It's more about the operations we can perform on the databases and tables within Glue data catalog.

You also cannot restrict this to specific table versions.

It applies to everything.

Now at a minimum, you need to have an IAM policy for Athena that grants access to the database and

to the Glue data catalog in each region.

But we can do more.

We can lock things down if we want to as well.

So you might have policies set up in IAM that might restrict access to alter or create database operations,

for creating new tables, for dropping databases or dropping tables for repairing tables, or even showing

all the databases or showing all of the tables.

So these are things you can lock down using IAM am and Athena.

Fine grained access to AWS Glue Catalog.

All you have to do is figure out the mapping of those operations to their underlying IAM actions, that

it's not always straightforward.

So here's an example on the right for controlling access to drop table.

Here we're saying we're going to allow the actions required to drop a table to a specific catalog,

to a specific database, and also to a specific table within that database.

And it turns out that to drop a table, you need access to get the partition and partitions that that

table is in.

You need to get the table and the database it's in, and you need permission to delete the table and

to delete the partition as well that it might be within.

So you kind of have to look up what those actions are for a given operation that is documented.

However, there's a little link there if you ever need to refer to it.

Can't imagine that they would expect you to memorize that stuff for the exam.

However, you can kind of use your common sense to figure out what actions might correspond to different

database operations that you might want to lock down using.

Athena.

Fine grained access to AWS Glue data catalog.